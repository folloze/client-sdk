import { FeatureName } from "../interfaces/IInitialState";
export declare function isFeatureEnabled(featureName: FeatureName): boolean;
export declare function areFeaturesEnabled(featureNames: FeatureName[]): boolean;
